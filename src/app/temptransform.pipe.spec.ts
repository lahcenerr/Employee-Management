import { TemptransformPipe } from './temptransform.pipe';

describe('TemptransformPipe', () => {
  it('create an instance', () => {
    const pipe = new TemptransformPipe();
    expect(pipe).toBeTruthy();
  });
});
